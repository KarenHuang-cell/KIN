# KIN Webflow — Next Steps

Running list of cleanup / improvement work to pick up after current section is shipped. Add new items as they surface.

---

## ✅ Nav — transparent + blur variant for overlay heroes (DONE 2026-05-11)

Shipped via the `nav-overlay` inline NavbarWrapper + `nav-blur-bg` child div + Footer Code scroll-state cascade. Details in `memory/project_webflow_nav_build.md`. Componentization as `Nav — Overlay` still pending — currently duplicated on About + Home.

---

## Responsive pass — only remaining build work (2026-05-13)

All other build work has shipped. Outstanding pages:
- **About**
- **Corporate Information**
- **About Mission**

Services pages already done. Once these three are through their breakpoints, the build phase closes and the site is launch-ready (modulo CSS cleanup tracked in `css-cleanup.md`).

---
